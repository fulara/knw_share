//for user it seems that all is nicely localized in one package, no need for multitude of includes.
use super::lib::*;


#[test]
fn foo() {
    let x = PublicEnum::A;
    let x = SomeStruct {};

    //use super::lib::
}
